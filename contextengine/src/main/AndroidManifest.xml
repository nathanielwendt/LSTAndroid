<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nathanielwendt.contextengine" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.ut.mpc.contextengine.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="com.ut.mpc.contextengine.PacoEngine"
            android:label="Paco Engine">
        </service>

        <service
            android:name="com.ut.mpc.contextengine.cabs.AbstractLocation"
            android:label="Abstract Location">
        </service>

        <service
            android:name="com.ut.mpc.contextengine.cabs.Safety"
            android:label="Safety">
        </service>

    </application>

</manifest>
